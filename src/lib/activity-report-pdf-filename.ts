/**
 * Nom de fichier suggéré pour un rapport d’activité PDF (aperçu / téléchargement).
 */
export function activityReportPdfFilename(year: number): string {
  return `rapport-activite-${year}.pdf`
}
